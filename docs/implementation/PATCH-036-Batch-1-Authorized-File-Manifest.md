# PATCH-036 Batch 1 Authorized File Manifest

Batch 1 — Frontend Foundation (S01), accepted under standing Human authority.

Authorized CREATE boundary: `frontend/package.json`, `package-lock.json`,
TypeScript/Vite configuration, `index.html`, `.gitignore`, `src/main.tsx`,
`src/App.tsx`, `src/api/*`, `src/auth/*`, `src/components/*`, `src/styles.css`,
and foundation tests. Purpose: toolchain, session-only authentication, typed
API mapping, design tokens/primitives, shell, routing, loading/protected/error
states. No backend, canonical state, dashboard customization, or future route
behavior. Stop on client-derived authority, unsupported API, or external
credential requirement.
