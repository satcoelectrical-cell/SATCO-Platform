import type { XDIAssessment, XDIReadiness } from "../../api/types";
export function CrossDisciplineOverviewMatrix({readiness,assessments,workspaceIds}:{readiness:XDIReadiness|null;assessments:XDIAssessment[];workspaceIds:number[]}) { return <section className="cross-discipline-card" aria-label="Cross-discipline overview"><h3>Overview</h3><p>{readiness?.state??"unavailable"} · {workspaceIds.length} authorized Workspaces</p><p>{assessments.length?assessments.length+" visible assessment references":"No authorized assessments."}</p>{readiness?.definition_digest?<code className="cross-discipline-machine-id">{readiness.definition_digest}</code>:null}</section>; }
